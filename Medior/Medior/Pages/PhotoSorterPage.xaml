<Page
    x:Class="Medior.Pages.PhotoSorterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Medior.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:psModels="using:Medior.Core.PhotoSorter.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!-- Profile Bar -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>


            <ComboBox Grid.Column="0" 
                      PlaceholderText="Select or create a sort job"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      ItemsSource="{x:Bind ViewModel.SortJobs}"
                      SelectedItem="{x:Bind ViewModel.SelectedJob, Mode=TwoWay}"
                      VerticalAlignment="Center">
            </ComboBox>

            <CommandBar Grid.Column="1" DefaultLabelPosition="Right" HorizontalAlignment="Left">
                <AppBarButton Label="New" Icon="Add" Click="AddJobButton_Click" />
                <AppBarButton Label="Rename" Icon="Rename" Click="RenameButton_Click" />
                <AppBarButton Label="Delete" Icon="Delete" Click="DeleteButton_Click" />
            </CommandBar>
        </Grid>

        <Grid Grid.Row="1">
            <Grid ColumnSpacing="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                
                
            </Grid>
        </Grid>
    </Grid>
</Page>
